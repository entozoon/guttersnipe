{
  "name": "guttersnipe",
  "private": false,
  "version": "0.1.4",
  "type": "module",
  "scripts": {
    "start": "npm-run-all -p start:*",
    "start:ui": "vite preview",
    "start:api": "node server",
    "dev": "npm-run-all -p dev:*",
    "dev:ui": "vite",
    "dev:api": "nodemon server",
    "build": "npm-run-all -p build:*",
    "build:ui": "vite build",
    "build:api": "tsc",
    "publish": "git add --all; git commit -m \"Early package dev..\"; npm version patch; git push; npm publish",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "connect-livereload": "^0.6.1",
    "express": "^4.18.2",
    "livereload": "^0.9.3",
    "npm-run-all": "^4.1.5",
    "preact": "^10.11.3",
    "vite": "^4.1.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.5.0",
    "nodemon": "^2.0.20",
    "typescript": "^4.9.3"
  }
}
